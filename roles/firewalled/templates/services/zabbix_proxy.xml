<?xml version="1.0" encoding="utf-8"?>
{{ ansible_managed | comment('xml') }}
<service>
    <short>zabbix_proxy</short>
    <description>Listens for connections from zabbix agents</description>
    <port protocol="tcp" port="{{ zabbix_proxy_active_listenport }}"/>
</service>
