{% for route in nic2_routes_list %}
ADDRESS{{ loop.index }}={{route.network | ansible.netcommon.ipaddr('address')}}
NETMASK{{ loop.index }}={{ route.network | ansible.netcommon.ipaddr('netmask')}}
GATEWAY{{ loop.index }}={{route.gw}}
METRIC{{ loop.index }}={{route.metric| default('102')}}
{% endfor %}
